javascript:(function(){ document.querySelectorAll('#HazeyGUI,#HazeyGUI-style').forEach(e=>e.remove()); const STYLE = document.createElement('style'); STYLE.id = 'HazeyGUI-style'; STYLE.textContent = ` @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;900&family=Titan+One&display=swap'); #HazeyGUI { --bg:#141414; --surface:#1e1e1e; --border:#2e2e2e; --accent:#0bc2cf; --accent2:#9a49aa; --enabled:#47A547; --disabled:#A02626; --text:#f0f0f0; --subtext:#999; --input-bg:#252525; --panel-w:250px; --header-h:44px; font-family:'Nunito',sans-serif; position:fixed; top:80px; left:80px; width:840px; height:570px; max-width:96vw; max-height:90vh; background:var(--bg); border:1px solid var(--border); border-radius:14px; box-shadow:0 24px 64px rgba(0,0,0,.75); display:flex; flex-direction:column; z-index:999999; overflow:hidden; resize:both; } #hgui-titlebar { display:flex; align-items:center; height:var(--header-h); padding:0 12px; background:var(--surface); border-bottom:1px solid var(--border); cursor:move; gap:10px; flex-shrink:0; } #hgui-hamburger { width:32px; height:32px; display:flex; flex-direction:column; justify-content:center; align-items:center; gap:5px; cursor:pointer; border-radius:6px; flex-shrink:0; transition:background .15s; } #hgui-hamburger:hover{background:var(--border);} #hgui-hamburger span{ display:block; width:18px; height:2px; background:var(--text); border-radius:2px; transition:transform .25s,opacity .25s; } #HazeyGUI.panel-open #hgui-hamburger span:nth-child(1){transform:translateY(7px) rotate(45deg);} #HazeyGUI.panel-open #hgui-hamburger span:nth-child(2){opacity:0;} #HazeyGUI.panel-open #hgui-hamburger span:nth-child(3){transform:translateY(-7px) rotate(-45deg);} #hgui-title { font-family:'Titan One',sans-serif; font-size:17px; color:var(--accent); letter-spacing:.5px; flex:1; pointer-events:none; } .hgui-wbtn { width:28px; height:28px; border-radius:50%; border:none; display:flex; align-items:center; justify-content:center; cursor:pointer; font-size:14px; font-weight:900; color:#000; transition:filter .15s; flex-shrink:0; } .hgui-wbtn:hover{filter:brightness(1.25);} #hgui-min{background:#f5a623;} #hgui-close{background:#e74c3c;} #hgui-body{display:flex;flex:1;overflow:hidden;position:relative;} #hgui-panel { position:absolute; top:0; left:0; width:var(--panel-w); height:100%; background:var(--surface); border-right:1px solid var(--border); display:flex; flex-direction:column; transform:translateX(-100%); transition:transform .25s cubic-bezier(.4,0,.2,1); z-index:10; overflow-y:auto; } #HazeyGUI.panel-open #hgui-panel{ transform:translateX(0); box-shadow:6px 0 24px rgba(0,0,0,.5); } .hgui-sec-label { font-size:10px; font-weight:700; letter-spacing:1.5px; text-transform:uppercase; color:var(--subtext); padding:14px 16px 6px; } .hgui-nav-btn { display:flex; align-items:center; gap:10px; padding:9px 16px; border-radius:8px; margin:2px 8px; cursor:pointer; font-size:13px; font-weight:700; color:var(--text); border:none; background:transparent; text-align:left; transition:background .15s,color .15s; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; } .hgui-nav-btn:hover{background:rgba(11,194,207,.12);color:var(--accent);} .hgui-nav-btn.active{background:rgba(11,194,207,.2);color:var(--accent);} .hgui-nav-icon{width:20px;height:20px;object-fit:contain;flex-shrink:0;} #hgui-backdrop{ display:none; position:absolute; inset:0; z-index:9; background:rgba(0,0,0,.4); } #HazeyGUI.panel-open #hgui-backdrop{display:block;} #hgui-main{flex:1;display:flex;flex-direction:column;overflow:hidden;} #hgui-content-header{ display:flex; align-items:center; padding:10px 18px 8px; border-bottom:1px solid var(--border); flex-shrink:0; } #hgui-content-title{ font-family:'Titan One',sans-serif; font-size:20px; color:var(--text); } #hgui-cheat-grid{ flex:1; overflow-y:auto; padding:14px 16px; display:flex; flex-direction:row; flex-wrap:wrap; align-content:flex-start; gap:10px; } #hgui-cheat-grid::-webkit-scrollbar{width:6px;} #hgui-cheat-grid::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px;} .hgui-card{ width:210px; background:var(--surface); border:1px solid var(--border); border-radius:10px; padding:12px 13px 13px; display:flex; flex-direction:column; gap:8px; box-shadow:inset 0 -4px rgba(0,0,0,.25); transition:border-color .15s,transform .1s; } .hgui-card:hover{border-color:var(--accent);transform:translateY(-1px);} .hgui-card-name{ font-size:13px; font-weight:700; color:var(--text); line-height:1.35; } .hgui-card-desc{ font-size:11px; color:var(--subtext); line-height:1.4; margin-top:-4px; } .hgui-run-btn{ align-self:flex-end; padding:5px 14px; border-radius:6px; border:none; background:var(--accent2); color:#fff; font-family:'Nunito',sans-serif; font-weight:700; font-size:12px; cursor:pointer; box-shadow:inset 0 -3px rgba(0,0,0,.25); transition:filter .15s,transform .1s; margin-top:auto; } .hgui-run-btn:hover{filter:brightness(1.15);} .hgui-run-btn:active{transform:translateY(1px);box-shadow:none;} .hgui-run-btn.ton{background:var(--enabled);} .hgui-run-btn.toff{background:var(--disabled);} .hgui-card input,.hgui-card select{ width:100%; padding:5px 8px; border-radius:6px; border:1px solid var(--border); background:var(--input-bg); color:var(--text); font-family:'Nunito',sans-serif; font-size:12px; box-sizing:border-box; } .hgui-card input:focus,.hgui-card select:focus{outline:1px solid var(--accent);} .hgui-card input[type=number]::-webkit-inner-spin-button{-webkit-appearance:none;} .hgui-run-err{font-size:11px;color:#e74c3c;margin-top:2px;word-break:break-word;} .hgui-widget{width:100%;box-sizing:border-box;} #HazeyGUI.minimised{height:var(--header-h)!important;resize:none;overflow:hidden;} #HazeyGUI.minimised #hgui-body{display:none;} `; document.head.appendChild(STYLE); const _wfc = window.fetch.call; window.fetch.call = function(){ if(!arguments[1]?.includes?.('s.blooket.com/rc')) return _wfc.apply(this,arguments); }; const Settings = { data: {}, setItem(k,v){ k.split('.').reduce((o,a,i,ar)=>(++i==ar.length&&(o[a]=v),o[a]??={},o[a]),this.data); localStorage.setItem('ZobroGUISettings',JSON.stringify(this.data)); return this.data; }, setData(v){ this.data=v; localStorage.setItem('ZobroGUISettings',JSON.stringify(this.data)); } }; try{ Settings.data = JSON.parse(localStorage.getItem('ZobroGUISettings')||'{}'); }catch{ Settings.setData({}); } function el(tag, props={}, ...kids){ const e = document.createElement(tag); for(const k in props){ if(k==='style'&&typeof props[k]==='object') Object.assign(e.style,props[k]); else e[k]=props[k]; } kids.forEach(k=>k&&e.append(k)); return e; } function getStateNode(){ return Object.values( (function r(n=document.querySelector('body>div')){ return Object.values(n)[1]?.children?.[0]?._owner.stateNode ? n : r(n.querySelector(':scope>div')); })() )[1].children[0]._owner.stateNode; } let _iAlert, _iPrompt, _iConfirm; (function(){ const f = document.createElement('iframe'); document.body.appendChild(f); _iAlert = f.contentWindow.alert.bind(window); _iPrompt = f.contentWindow.prompt.bind(window); _iConfirm = f.contentWindow.confirm.bind(window); f.remove(); })(); const C = { global:[ { name:'Auto Answer', description:'Toggles auto answer on', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(),q=s.state.question||s.props.client.question; if(q.qType!=='typing'){ if(s.state.stage!=='feedback'&&!s.state.feedback){ const i=q.answers.findIndex(a=>q.correctAnswers.includes(a)); document.querySelectorAll('[class*="answerContainer"]')[i]?.click(); }else document.querySelector('[class*="feedback"],[id*="feedback"]')?.firstChild?.click(); }else Object.values(document.querySelector('[class*="typingAnswerWrapper"]'))[1].children._owner.stateNode.sendAnswer(q.answers[0]); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Highlight Answers', description:'Colors answers green/red', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(),q=s.state.question||s.props.client.question; [...document.querySelectorAll('[class*="answerContainer"]')].forEach((a,i)=>{ a.style.backgroundColor=q.correctAnswers.includes(q.answers[i])?'rgb(0,207,119)':'rgb(189,15,38)'; }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Subtle Highlight', description:'Removes shadow from correct answers', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(),q=s.state.question||s.props.client.question; [...document.querySelectorAll('[class*="answerContainer"]')].forEach((a,i)=>{ if(q.correctAnswers.includes(q.answers[i])) a.style.boxShadow='unset'; }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Percent Auto Answer', description:'Answers to hit a target grade %', type:'toggle', enabled:false, data:null, inputs:[{name:'Target Grade',type:'number'}], run:function(target){ if(!this.enabled){ this.enabled=true; const s=getStateNode(); this.data=setInterval(t=>{ try{ const q=s.state.question||s.props.client.question; if(s.state.stage==='feedback'||s.state.feedback){ document.querySelector('[class*="feedback"],[id*="feedback"]')?.firstChild?.click?.(); return; } let cor=0,tot=0; for(let k in s.corrects) cor+=s.corrects[k]; for(let k in s.incorrects) tot+=s.incorrects[k]; tot+=cor; const yes=tot===0||Math.abs(cor/(tot+1)-t)>=Math.abs((cor+1)/(tot+1)-t); if(q.qType!=='typing'){ const els=document.querySelectorAll('[class*="answerContainer"]'); for(let i=0;i<els.length;i++){ const c=q.correctAnswers.includes(q.answers[i]); if(yes===c){els[i]?.click?.();return;} } els[0]?.click?.(); }else{ Object.values(document.querySelector('[class*="typingAnswerWrapper"]'))[1].children._owner.stateNode.sendAnswer(yes?q.answers[0]:Math.random().toString(36).slice(2)); } }catch{} },100,(target??100)/100); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Every Answer Correct', description:'Sets every answer to be correct', run:function(){ try{ const s=getStateNode(); s.freeQuestions?.forEach?.((q,i)=>{ q.correctAnswers=q.answers; s.questions[i].correctAnswers=q.answers; s.props.client.questions[i].correctAnswers=q.answers; }); s.forceUpdate?.(); }catch(e){ _iAlert('Error: '+e.message); } } }, { name:'Remove Random Name', description:'Allows a custom name', run:function(){ try{ getStateNode().setState({isRandom:false,client:{name:''}}); document.querySelector('[class*="nameInput"]')?.focus?.(); }catch{} } }, { name:'Use Any Blook', description:'Play as any blook (lobby or blooks page)', run:function(){ try{ const s=getStateNode(); if(s.state.blookData){ let orig=Object.entries; Object.entries=function(e){ if(e?.Chick){ const u={}; Object.keys(e).forEach(k=>u[k]=s.state.blookData[k]||1); s.setState({blookData:u}); Object.entries=orig; } return orig.apply(this,arguments); }; s.render(); }else s.setState({unlocks:{includes:()=>true}}); }catch(e){ _iAlert('Run this on the lobby or blooks page.'); } } }, { name:'Change Blook Ingame', description:'Changes your blook during a game', inputs:[{name:'Blook Name',type:'string'}], run:function(blook){ try{ const p=getStateNode().props; p.client.blook=blook; p.liveGameController.setVal({path:'c/'+p.client.name+'/b',val:blook}); }catch(e){ _iAlert(e.message); } } }, { name:'Host Any Gamemode', description:'Change gamemode on host settings page', inputs:[{name:'Gamemode',type:'options',options:['Racing','Classic','Factory','Cafe','Defense2','Defense','Royale','Gold','Candy','Brawl','Hack','Pirate','Fish','Dino','Toy','Rush']}], run:function(type){ try{ if(location.pathname!=='/host/settings'){_iAlert('Run on host settings page');return;} getStateNode().setState({settings:{type}}); }catch(e){ _iAlert(e.message); } } }, { name:'Get Daily Rewards', description:'Gets max daily tokens + XP', run:async function(){ if(!location.href.includes('play.blooket.com')){ _iAlert('Only works on play.blooket.com'); window.open('https://play.blooket.com/'); return; } const ids=['60101da869e8c70013913b59','625db660c6842334835cb4c6','60268f8861bd520016eae038','611e6c804abdf900668699e3','60ba5ff6077eb600221b7145','642467af9b704783215c1f1b','605bd360e35779001bf57c5e','6234cc7add097ff1c9cff3bd','600b1491d42a140004d5215a','5db75fa3f1fa190017b61c0c']; const rand=(l,h)=>Math.floor(Math.random()*(h-l+1))+l; const {t}=await fetch('https://play.blooket.com/api/playersessions/solo',{body:JSON.stringify({gameMode:'Factory',questionSetId:ids[Math.floor(Math.random()*ids.length)]}),method:'POST',credentials:'include'}).then(r=>r.json()); await fetch('https://play.blooket.com/api/playersessions/landings',{body:JSON.stringify({t}),method:'POST',credentials:'include'}); await fetch('https://play.blooket.com/api/playersessions/questions?t='+t,{credentials:'include'}); await fetch('https://play.blooket.com/api/users/factorystats',{body:JSON.stringify({t,place:1,cash:rand(10000000,100000000),playersDefeated:0,correctAnswers:rand(500,2000),upgrades:rand(250,750),blookUsed:'Chick',nameUsed:'You',mode:'Time-Solo'}),method:'PUT',credentials:'include'}); const res=await fetch('https://play.blooket.com/api/users/add-rewards',{body:JSON.stringify({t,addedTokens:500,addedXp:300}),method:'PUT',credentials:'include'}).then(r=>r.json()); _iAlert('Added 500 tokens, 300 XP, and '+res.dailyReward+' daily wheel tokens!'); } }, { name:'Bypass Name Filter', description:'Copies filter-bypassed text to clipboard', inputs:[{name:'Text',type:'string'}], run:function(text){ const b=text.split('').map(c=>'\u00ad'+c).join(''); const t=document.createElement('textarea'); t.value=b; t.style.cssText='position:fixed;top:0;left:0;opacity:0'; document.body.appendChild(t); t.select(); document.execCommand('copy'); document.body.removeChild(t); _iAlert('Bypassed text copied!'); } }, { name:'Sell Duplicate Blooks', description:'Sell dupes (no legendaries+)', run:async function(){ if(!/dashboard.*\/blooks/.test(location.href)){_iAlert('Run on the Blooks page');return;} if(!_iConfirm('Sell all dupes? Legendaries+ skipped.')){return;} const s=getStateNode(); let res=''; for(const b in s.state.blookData){ if(s.state.blookData[b]>1){ s.setState({blook:b,numToSell:s.state.blookData[b]-1}); if(['Legendary','Chroma','Mystical'].includes(document.querySelector('[class*="highlightedRarity"]')?.innerText?.trim())) continue; res+=b+' x'+(s.state.blookData[b]-1)+'\n'; await s.sellBlook({preventDefault(){}},true); } } _iAlert('Results:\n'+res.trim()); } }, { name:'Spam Buy Blooks', description:'Opens a box multiple times', inputs:[{name:'Box',type:'options',options:()=>Array.from(document.querySelectorAll('[class*="packsWrapper"] > div')).reduce((a,b)=>(b.querySelector('[class*="blookContainer"] > img')||a.push(b.querySelector('[class*="packImgContainer"] > img')?.alt),a),[]).filter(Boolean)},{name:'Amount',type:'number'}], run:async function(box,amount){ if(!location.pathname.startsWith('/market')){_iAlert('Run on Market page');return;} const s=getStateNode(); box=box.split(' ').map(w=>w[0].toUpperCase()+w.slice(1).toLowerCase()).join(' '); const prices=Array.from(document.querySelectorAll('[class*="packsWrapper"] > div')).reduce((a,b)=>{const img=b.querySelector('[class*="packImgContainer"] > img'); if(img) a[img.alt]=parseInt(b.querySelector('[class*="packBottom"]')?.textContent); return a;},{}); const cost=prices[box]; if(!cost){_iAlert('Box not found!');return;} const n=Math.min(Math.floor(s.state.tokens/cost),amount||0); if(!n){_iAlert('Not enough tokens!');return;} const blooks={}; for(let i=0;i<n;i++){ await s.buyPack(true,box); blooks[s.state.unlockedBlook]=(blooks[s.state.unlockedBlook]||0)+1; s.startOpening?.(); clearTimeout(s.openTimeout); s.setState({canOpen:true,currentPack:'',opening:false,doneOpening:false,openPack:false}); clearTimeout(s.canOpenTimeout); } await new Promise(r=>setTimeout(r)); _iAlert('Results:\n'+Object.entries(blooks).map(([b,c])=>b+' x'+c).join('\n')); } }, ], gold:[ { name:'Always Triple', description:'Always get triple gold', type:'toggle', enabled:false, data:null, run:function(){ const s=getStateNode(); s._choosePrize??=s.choosePrize; if(!this.enabled){ this.enabled=true; s.choosePrize=i=>{ s.state.choices[i]={type:'multiply',val:3,text:'Triple Gold!',blook:'Unicorn'}; s._choosePrize(i); }; }else{ this.enabled=false; s.choosePrize=s._choosePrize; } } }, { name:'Always Quintuple', description:'Always get quintuple gold', type:'toggle', enabled:false, data:null, run:function(){ const s=getStateNode(); s._choosePrize??=s.choosePrize; if(!this.enabled){ this.enabled=true; s.choosePrize=i=>{ s.state.choices[i]={type:'multiply',val:5,text:'Quintuple Gold!',blook:'Ice Elemental'}; s._choosePrize(i); }; }else{ this.enabled=false; s.choosePrize=s._choosePrize; } } }, { name:'Chest ESP', description:'Shows what each chest contains', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ getStateNode().state.choices.forEach(({text},i)=>{ const c=document.querySelector('div[class*="choice'+(i+1)+'"]'); if(!c||c.querySelector('div')) return; const d=document.createElement('div'); d.style.cssText='color:white;font-family:Eczar;font-size:2em;display:flex;justify-content:center;transform:translateY(200px)'; d.innerText=text; c.append(d); }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Auto Choose', description:'Auto-picks the best gold option', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); if(s.state.stage!=='prize') return; s.props.liveGameController.getDatabaseVal('c',players=>{ if(!players) return; const most=Math.max(...Object.entries(players).filter(([n])=>n!==s.props.client.name).map(([,v])=>v||0),0); let max=0,idx=-1; s.state.choices.forEach((c,i)=>{ let v=s.state.gold; if(c.type==='gold') v=s.state.gold+(c.val||0); else if(c.type==='multiply'||c.type==='divide') v=Math.round(s.state.gold*c.val)||s.state.gold; else if(c.type==='swap') v=most||s.state.gold; else if(c.type==='take') v=s.state.gold+most*(c.val||0); if((v||0)>max){max=v;idx=i+1;} }); document.querySelector('div[class*="choice'+idx+'"]')?.click(); }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Set Gold', description:'Sets your gold amount', inputs:[{name:'Gold',type:'number'}], run:function(gold){ try{ const s=getStateNode(); s.setState({gold,gold2:gold}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/g',val:gold}); }catch(e){ _iAlert(e.message); } } }, { name:'Swap Gold', description:'Swaps gold with another player', inputs:[{name:'Player',type:'options',options(){ const s=getStateNode(); return new Promise(res=>s.props.liveGameController._liveApp?s.props.liveGameController.getDatabaseVal('c',p=>p&&res(Object.keys(p))):res([])); }}], run:function(player){ try{ const s=getStateNode(); s.props.liveGameController.getDatabaseVal('c',p=>{ if(!p?.[player]) return; const g=p[player].g||0; s.props.liveGameController.setVal({path:'c/'+s.props.client.name,val:{b:s.props.client.blook,tat:player+':swap:'+(s.state.gold||0),g}}); s.setState({gold:g,gold2:g}); }); }catch(e){ _iAlert(e.message); } } }, { name:'Reset Player Gold', description:"Sets a player's gold to 0", inputs:[{name:'Player',type:'options',options(){ const s=getStateNode(); return new Promise(res=>s.props.liveGameController._liveApp?s.props.liveGameController.getDatabaseVal('c',p=>p&&res(Object.keys(p))):res([])); }}], run:function(player){ try{ const s=getStateNode(); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/tat',val:player+':swap:0'}); }catch(e){ _iAlert(e.message); } } }, ], crypto:[ { name:'Always Triple Crypto', description:'Always get triple crypto', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ getStateNode().setState({choices:[{type:'mult',val:3,rate:.075,blook:'Brainy Bot',text:'Triple Crypto'}]}); }catch{} },25); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Password ESP', description:'Highlights the correct password', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); if(s.state.stage==='hack'){ for(const b of document.querySelector('div[class*=buttonContainer]').children){ if(b.innerText===s.state.correctPassword){b.style.outlineColor='';b.style.backgroundColor='';} else{b.style.outlineColor='rgba(255,64,64,.8)';b.style.backgroundColor='rgba(255,64,64,.8)';} } } }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Auto Guess Password', description:'Auto-clicks the correct password', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); if(s.state.stage==='hack'){ for(const b of document.querySelector('div[class*=buttonContainer]').children) if(b.innerText===s.state.correctPassword){b.click();break;} } }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Set Crypto', description:'Sets your crypto amount', inputs:[{name:'Amount',type:'number'}], run:function(amount){ try{ const s=getStateNode(); s.setState({crypto:amount,crypto2:amount}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/cr',val:amount}); }catch(e){ _iAlert(e.message); } } }, { name:'Remove Hack', description:'Removes an incoming hack', run:function(){ try{ getStateNode().setState({hack:''}); }catch(e){ _iAlert(e.message); } } }, { name:'Steal Crypto', description:"Steals a player's crypto", inputs:[{name:'Player',type:'options',options(){ const s=getStateNode(); return new Promise(res=>s.props.liveGameController._liveApp?s.props.liveGameController.getDatabaseVal('c',p=>p&&res(Object.keys(p))):res([])); }}], run:function(target){ try{ const s=getStateNode(); s.props.liveGameController.getDatabaseVal('c',p=>{ if(!p) return; const [name,{cr}]=Object.entries(p).find(([n])=>n.toLowerCase()===target.toLowerCase())||[]; if(cr==null) return; s.setState({crypto:s.state.crypto+cr,crypto2:s.state.crypto+cr}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name,val:{b:s.props.client.blook,p:s.state.password,cr:s.state.crypto+cr,tat:name+':'+cr}}); }); }catch(e){ _iAlert(e.message); } } }, ], fishing:[ { name:'Remove Distractions', description:'Removes all distractions', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ getStateNode().setState({party:''}); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Frenzy', description:'Sets everyone to frenzy mode', run:function(){ try{ const s=getStateNode(); s.props.liveGameController.setVal({path:'c/'+s.props.client.name,val:{b:s.props.client.blook,w:s.state.weight,f:'Frenzy',s:true}}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Weight', description:'Sets your fishing weight', inputs:[{name:'Weight',type:'number'}], run:function(weight){ try{ const s=getStateNode(); s.setState({weight,weight2:weight}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name,val:{b:s.props.client.blook,w:weight,f:['Crab','Jellyfish','Frog','Pufferfish','Octopus','Narwhal','Megalodon','Blobfish','Baby Shark'][Math.floor(Math.random()*9)]}}); }catch(e){ _iAlert(e.message); } } }, { name:'Send Distraction', description:'Sends a distraction to everyone', inputs:[{name:'Distraction',type:'options',options:['Crab','Jellyfish','Frog','Pufferfish','Octopus','Narwhal','Megalodon','Blobfish','Baby Shark']}], run:function(f){ try{ const s=getStateNode(); s.safe=true; s.props.liveGameController.setVal({path:'c/'+s.props.client.name,val:{b:s.props.client.blook,w:s.state.weight,f,s:true}}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Lure', description:'Sets fishing lure (1–5)', inputs:[{name:'Lure (1-5)',type:'number',min:1,max:5}], run:function(lure){ try{ getStateNode().setState({lure:Math.max(Math.min(lure-1,4),0)}); }catch(e){ _iAlert(e.message); } } }, ], voyage:[ { name:'Heist ESP', description:'Shows what is under each chest', type:'toggle', enabled:false, data:null, _imgs:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); if(s.state.stage!=='heist') return; if(!this._imgs) this._imgs=Array.from(document.querySelector('[class*=prizesList]').children).slice(1,4).map(x=>x.querySelector('img').src); const esp=Object.values(document.querySelector('[class*=modal]'))[0].return.memoizedState.memoizedState; const open=Object.values(document.querySelector('[class*=modal]'))[0].return.memoizedState.next.next.memoizedState; document.querySelectorAll('[class*=boxContent] > div').forEach(e=>e.remove()); Array.from(document.querySelector('[class*=chestsWrapper]').children).forEach((container,i)=>{ const box=container.firstChild.firstChild; if(open.includes(i)){box.style.opacity='';return;} box.style.opacity='.5'; const d=document.createElement('div'); d.innerHTML='<img src="'+this._imgs[2-esp[i]]+'" style="max-width:75%;max-height:75%">'; Object.assign(d.style,{position:'absolute',inset:'0',display:'grid',placeItems:'center',pointerEvents:'none',className:'chestESP'}); container.onclick=()=>{d.remove();box.style.opacity='';}; container.firstChild.prepend(d); }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; this._imgs=null; } } }, { name:'Max Levels', description:'Maxes all islands and boat', run:function(){ try{ const s=getStateNode(); s.setState({islandLevels:new Array(s.state.islandLevels.length).fill(5)},s.updateBoatLevel); }catch(e){ _iAlert(e.message); } } }, { name:'Set Doubloons', description:'Sets your doubloons', inputs:[{name:'Amount',type:'number'}], run:function(d){ try{ const s=getStateNode(); s.setState({doubloons:d}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/d',val:d}); }catch(e){ _iAlert(e.message); } } }, { name:'Take Doubloons', description:"Takes doubloons from a player", inputs:[{name:'Player',type:'options',options(){ const s=getStateNode(); return new Promise(res=>s.props.liveGameController._liveApp?s.props.liveGameController.getDatabaseVal('c',p=>p&&res(Object.keys(p))):res([])); }}], run:async function(target){ try{ const s=getStateNode(); const p=await new Promise(res=>s.props.liveGameController.getDatabaseVal('c',res)); const [name,data]=Object.entries(p).find(([n])=>n===target)||[]; if(!data) return; s.setState({doubloons:s.state.doubloons+data.d}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name,val:{b:s.props.client.blook,d:s.state.doubloons+data.d,tat:name+':'+data.d}}); }catch(e){ _iAlert(e.message); } } }, ], brawl:[ { name:'Double Enemy XP', description:'Doubles enemy XP drops', run:function(){ try{ const cols=getStateNode().game.current.config.sceneConfig.physics.world.colliders._active.filter(x=>x.callbackContext?.toString().includes('invulnerableTime')); for(const c of cols){ const e=c.object2; const _s=e.classType.prototype.start; e.classType.prototype.start=function(){_s.apply(this,arguments);this.val*=2;}; e.children.entries.forEach(x=>x.val*=2); } }catch(e){ _iAlert(e.message); } } }, { name:'Half Enemy Speed', description:'Makes enemies 2x slower', run:function(){ try{ const cols=getStateNode().game.current.config.sceneConfig.physics.world.colliders._active.filter(x=>x.callbackContext?.toString().includes('invulnerableTime')); for(const c of cols){ const e=c.object2; const _s=e.classType.prototype.start; e.classType.prototype.start=function(){_s.apply(this,arguments);this.speed*=.5;}; e.children.entries.forEach(x=>x.speed*=.5); } }catch(e){ _iAlert(e.message); } } }, { name:'Instant Kill', description:'Sets all enemies HP to 1', run:function(){ try{ const cols=getStateNode().game.current.config.sceneConfig.physics.world.colliders._active.filter(x=>x.callbackContext?.toString().includes('invulnerableTime')); for(const c of cols){ const e=c.object2; const _s=e.classType.prototype.start; e.classType.prototype.start=function(){_s.apply(this,arguments);this.hp=1;}; e.children.entries.forEach(x=>x.hp=1); } }catch(e){ _iAlert(e.message); } } }, { name:'Invincibility', description:'Makes you invincible', run:function(){ try{ getStateNode().game.current.config.sceneConfig.physics.world.colliders._active.filter(x=>x.callbackContext?.toString().includes('invulnerableTime')||x.callbackContext?.toString().includes('dmgCd')).forEach(c=>c.collideCallback=()=>{}); }catch(e){ _iAlert(e.message); } } }, { name:'Kill Enemies', description:'Kills all current enemies', run:function(){ try{ getStateNode().game.current.config.sceneConfig.physics.world.bodies.entries.forEach(x=>x?.gameObject?.receiveDamage?.(x.gameObject.hp,1)); }catch(e){ _iAlert(e.message); } } }, { name:'Max Current Abilities', description:'Maxes all current abilities', run:function(){ try{ const s=getStateNode(); for(const[a,lv]of Object.entries(s.state.abilities)) for(let i=0;i<10-lv;i++) s.game.current.config.sceneConfig.game.events.emit('level up',a,s.state.abilities[a]++); s.setState({level:[1,3,5,10,15,25,35].sort((a,b)=>Math.abs(a-s.state.level)-Math.abs(b-s.state.level))[0]-1}); }catch(e){ _iAlert(e.message); } } }, { name:'Next Level', description:'Skips to the next level', run:function(){ try{ const s=getStateNode(); const{object1:p,object2:x}=s.game.current.config.sceneConfig.physics.world.colliders._active.find(c=>c.collideCallback?.toString().includes('emit("xp')); x.get().spawn(p.x,p.y,((l=s.state.level)=>l===1?1:l<5?5:l<10?10:l<20?20:l<30?30:l<40?40:l<50?50:100)()-s.xp); }catch(e){ _iAlert(e.message); } } }, ], defense2:[ { name:'Max Tower Stats', description:'Makes placed towers overpowered', run:function(){ try{ getStateNode().state.towers.forEach(t=>{ t.stats.dmg=1e6; t.stats.fireRate=50; t.stats.ghostDetect=true; t.stats.maxTargets=1e6; if(t.stats.numProjectiles) t.stats.numProjectiles=100; t.stats.range=100; if(t.stats.auraBuffs) for(const k in t.stats.auraBuffs) t.stats.auraBuffs[k]*=100; }); }catch(e){ _iAlert(e.message); } } }, { name:'Kill Enemies', description:'Kills all enemies', run:function(){ try{ const s=getStateNode(); s.game.current.config.sceneConfig.enemyQueue?.length&&(s.game.current.config.sceneConfig.enemyQueue.length=0); s.game.current.config.sceneConfig.physics.world.bodies.entries.forEach(x=>x?.gameObject?.receiveDamage?.(x.gameObject.hp,1)); }catch(e){ _iAlert(e.message); } } }, { name:'Set Coins', description:'Sets your coins', inputs:[{name:'Coins',type:'number'}], run:function(coins){ try{ getStateNode().setState({coins}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Health', description:'Sets your health', inputs:[{name:'Health',type:'number'}], run:function(health){ try{ getStateNode().setState({health}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Round', description:'Sets the current round', inputs:[{name:'Round',type:'number'}], run:function(round){ try{ getStateNode().setState({round}); }catch(e){ _iAlert(e.message); } } }, ], dinos:[ { name:'Rock ESP', description:'Shows what is under the rocks', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); const rocks=[...document.querySelector('[class*="rockButton"]').parentElement.children]; if(rocks.every(r=>r.querySelector('div'))) return; s.setState({choices:[{type:'fossil',val:10,rate:.1,blook:'Amber'},{type:'fossil',val:25,rate:.1,blook:'Dino Egg'},{type:'fossil',val:50,rate:.175,blook:'Dino Fossil'},{type:'fossil',val:75,rate:.175,blook:'Stegosaurus'},{type:'fossil',val:100,rate:.15,blook:'Velociraptor'},{type:'fossil',val:125,rate:.125,blook:'Brontosaurus'},{type:'fossil',val:250,rate:.075,blook:'Triceratops'},{type:'fossil',val:500,rate:.025,blook:'Tyrannosaurus Rex'}].sort(()=>.5-Math.random()).slice(0,3)},()=>{ rocks.forEach((r,i)=>{ r.querySelector('div')?.remove(); const d=document.createElement('div'); const ch=s.state.choices[i]; d.style.cssText='color:white;font-size:1em;display:flex;justify-content:center;transform:translateY(25px)'; d.innerText=ch.type==='fossil'?'+'+Math.round(ch.val*s.state.fossilMult)+' Fossils':'x'+ch.val+' Fossils/Exc'; r.append(d); }); }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Auto Choose Fossil', description:'Auto-picks the best fossil', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); if(s.state.stage!=='excavate') return; let max=0,idx=-1; s.state.choices.forEach((c,i)=>{ const v=(c.type==='fossil'?s.state.fossils+c.val*s.state.fossilMult:s.state.fossils*c.val)||0; if(v>max||c.type==='mult'){max=v;idx=i+1;} }); document.querySelector('div[class*=rockRow] > div[role="button"]:nth-child('+idx+')')?.click(); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, { name:'Set Fossils', description:'Sets your fossil count', inputs:[{name:'Fossils',type:'number'}], run:function(f){ try{ const s=getStateNode(); s.setState({fossils:f}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/f',val:f}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Multiplier', description:'Sets fossil multiplier', inputs:[{name:'Multiplier',type:'number'}], run:function(m){ try{ getStateNode().setState({fossilMult:m}); }catch(e){ _iAlert(e.message); } } }, { name:'Stop Cheating Flag', description:"Hides the cheating flag from the host", run:function(){ try{ const s=getStateNode(); s.setState({isCheating:false}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/ic',val:false}); }catch(e){ _iAlert(e.message); } } }, ], cafe:[ { name:'Max Items', description:'Max all shop items (shop page)', run:function(){ try{ if(location.pathname!=='/cafe/shop'){_iAlert('Run in the shop');return;} const s=getStateNode(); s.setState({items:Object.fromEntries(Object.entries(s.state.items).map(([k])=>[k,5]))}); }catch(e){ _iAlert(e.message); } } }, { name:'Remove Customers', description:'Skips current customers', run:function(){ try{ const s=getStateNode(); s.state.customers.forEach((c,i)=>c.blook&&s.removeCustomer(i,true)); }catch(e){ _iAlert(e.message); } } }, { name:'Stock Food', description:'Stocks all food to 99', run:function(){ try{ if(location.pathname!=='/cafe'){_iAlert("Run in the cafe (not shop)");return;} const s=getStateNode(); s.setState({foods:s.state.foods.map(f=>({...f,stock:99,level:5}))}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Cash', description:'Sets your cafe cash', inputs:[{name:'Amount',type:'number'}], run:function(ca){ try{ const s=getStateNode(); s.setState({cafeCash:ca}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/ca',val:ca}); }catch(e){ _iAlert(e.message); } } }, ], factory:[ { name:'Free Upgrades', description:'Sets all upgrade prices to 0', run:function(){ try{ const s=getStateNode(); s.setState({blooks:s.state.blooks.map(b=>({...b,price:[0,0,0,0]}))}); }catch(e){ _iAlert(e.message); } } }, { name:'Max Blooks', description:'Maxes all blook levels', run:function(){ try{ getStateNode().state.blooks.forEach(b=>b.level=4); }catch(e){ _iAlert(e.message); } } }, { name:'Set All MegaBot', description:'Fills slots with maxed Mega Bots', run:function(){ try{ getStateNode().setState({blooks:Array(10).fill({name:'Mega Bot',color:'#d71f27',class:'\uD83E\uDD16',rarity:'Legendary',cash:[80000,430000,4200000,62000000,1000000000],time:[5,5,3,3,3],price:[7000000,120000000,1900000000,35000000000],active:false,level:4,bonus:5.5})}); }catch(e){ _iAlert(e.message); } } }, { name:'Remove Glitches', description:'Removes all enemy glitches', run:function(){ try{ const s=getStateNode(); s.setState({bits:0,ads:[],hazards:[],color:'',lol:false,joke:false,slow:false,dance:false,glitch:'',glitcherName:'',glitcherBlook:''}); ['adTimeout','hazardInterval','nightTimeout','glitchTimeout','lolTimeout','jokeTimeout','slowTimeout','danceTimeout','nameTimeout'].forEach(k=>clearTimeout(s[k])); }catch(e){ _iAlert(e.message); } } }, { name:'Set Cash', description:'Sets your factory cash', inputs:[{name:'Cash',type:'number'}], run:function(cash){ try{ getStateNode().setState({cash}); }catch(e){ _iAlert(e.message); } } }, { name:'Send Glitch', description:'Sends a glitch to all players', inputs:[{name:'Glitch',type:'options',options:[{name:'Lunch Break',value:'lb'},{name:'Ad Spam',value:'as'},{name:'Error 37',value:'e37'},{name:'Night Time',value:'nt'},{name:'#LOL',value:'lo'},{name:'Jokester',value:'j'},{name:'Slow Mo',value:'sm'},{name:'Dance Party',value:'dp'},{name:'Vortex',value:'v'},{name:'Reverse',value:'r'},{name:'Flip',value:'f'},{name:'Micro',value:'m'}]}], run:function(val){ try{ const s=getStateNode(); s.safe=true; s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/tat',val}); }catch(e){ _iAlert(e.message); } } }, ], racing:[ { name:'Instant Win', description:'Instantly wins the race', run:function(){ try{ const s=getStateNode(); s.setState({progress:s.state.goalAmount}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/pr',val:s.state.goalAmount}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Progress', description:'Sets questions remaining', inputs:[{name:'Questions',type:'number'}], run:function(q){ try{ const s=getStateNode(); const p=s.props.client.amount-q; s.setState({progress:p}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/pr',val:p}); }catch(e){ _iAlert(e.message); } } }, ], royale:[ { name:'Auto Answer', description:'Toggles auto answer on', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); s?.onAnswer?.(true,s.props.client.question.correctAnswers[0]); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); this.data=null; } } }, ], defense:[ { name:'Max Tower Stats', description:'Makes all placed towers overpowered', run:function(){ try{ getStateNode().towers.forEach(t=>{t.range=100;t.fullCd=t.cd=0;t.damage=1e6;}); }catch(e){ _iAlert(e.message); } } }, { name:'Remove Enemies', description:'Removes all enemies', run:function(){ try{ const s=getStateNode(); s.enemies=s.futureEnemies=[]; }catch(e){ _iAlert(e.message); } } }, { name:'Remove Obstacles', description:'Lets you place towers anywhere', run:function(){ try{ const s=getStateNode(); s.tiles=s.tiles.map(r=>r.fill(0)); }catch(e){ _iAlert(e.message); } } }, { name:'Set Tokens', description:'Sets your tokens', inputs:[{name:'Tokens',type:'number'}], run:function(tokens){ try{ getStateNode().setState({tokens}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Round', description:'Sets the current round', inputs:[{name:'Round',type:'number'}], run:function(round){ try{ getStateNode().setState({round}); }catch(e){ _iAlert(e.message); } } }, ], kingdom:[ { name:'Max Stats', description:'Sets all resources to max', run:function(){ try{ getStateNode().setState({materials:100,people:100,happiness:100,gold:100}); }catch(e){ _iAlert(e.message); } } }, { name:'Disable Tax Toucan', description:'Prevents tax events', run:function(){ try{ getStateNode().taxCounter=Number.MAX_VALUE; }catch(e){ _iAlert(e.message); } } }, { name:'Set Guests', description:'Sets guests seen count', inputs:[{name:'Guests',type:'number'}], run:function(g){ try{ getStateNode().setState({guestScore:g}); }catch(e){ _iAlert(e.message); } } }, { name:'Skip Guest', description:'Skips the current guest', run:function(){ try{ getStateNode().nextGuest(); }catch(e){ _iAlert(e.message); } } }, { name:'Choice ESP', description:'Shows Yes/No outcomes', type:'toggle', enabled:false, data:null, run:function(){ if(!this.enabled){ this.enabled=true; this.data=setInterval(()=>{ try{ const s=getStateNode(); if(s.state.phase!=='choice') return; const statEls={}; document.querySelectorAll('[class*=statContainer]').forEach((e,i)=>statEls[['materials','people','happiness','gold'][i]]=e); document.querySelectorAll('.choiceESP').forEach(e=>e.remove()); Object.entries(s.state.guest.yes||{}).forEach(([k,v])=>{ if(!statEls[k]) return; const d=document.createElement('div'); d.className='choiceESP'; d.style.cssText='font-size:24px;color:rgb(75,194,46);font-weight:bolder'; d.innerText=String(v); statEls[k].appendChild(d); }); Object.entries(s.state.guest.no||{}).forEach(([k,v])=>{ if(!statEls[k]) return; const d=document.createElement('div'); d.className='choiceESP'; d.style.cssText='font-size:24px;color:darkred;font-weight:bolder'; d.innerText=String(v); statEls[k].appendChild(d); }); }catch{} },50); }else{ this.enabled=false; clearInterval(this.data); document.querySelectorAll('.choiceESP').forEach(e=>e.remove()); this.data=null; } } }, ], workshop:[ { name:'Remove Distractions', description:'Removes all distractions', run:function(){ try{ getStateNode().setState({fog:false,dusk:false,wind:false,plow:false,blizzard:false,force:false,canada:false,trees:Array(10).fill(false)}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Toys', description:'Sets your toy count', inputs:[{name:'Toys',type:'number'}], run:function(toys){ try{ const s=getStateNode(); s.setState({toys}); s.props.liveGameController.setVal({path:'c/'+s.props.client.name+'/t',val:toys}); }catch(e){ _iAlert(e.message); } } }, { name:'Set Toys Per Question', description:'Sets toys earned per question', inputs:[{name:'Per Q',type:'number'}], run:function(toysPerQ){ try{ getStateNode().setState({toysPerQ}); }catch(e){ _iAlert(e.message); } } }, ], flappy:[ { name:'Toggle Ghost', description:'Fly through pipes', type:'toggle', enabled:false, run:function(){ this.enabled=!this.enabled; try{ for(const b of Object.values(document.querySelector('#phaser-bouncy'))[0].return.updateQueue.lastEffect.deps[0].current.config.sceneConfig.physics.world.bodies.entries){ if(!b.gameObject.frame.texture.key.startsWith('blook')) continue; b.checkCollision.none=this.enabled; b.gameObject.setAlpha(this.enabled?.5:1); break; } }catch(e){ _iAlert(e.message); } } }, { name:'Set Score', description:'Sets the flappy score', inputs:[{name:'Score',type:'number'}], run:function(score){ try{ Object.values(document.querySelector('#phaser-bouncy'))[0].return.updateQueue.lastEffect.deps[1](score||0); }catch(e){ _iAlert(e.message); } } }, ], extras:[ { name:'Toggle Dark Mode', description:'Inverts the page colours', run:function(){ const id='_hazeyDark'; if(document.getElementById(id)){document.getElementById(id).remove();return;} const s=document.createElement('style'); s.id=id; s.textContent='html{-webkit-filter:invert(100%) hue-rotate(180deg) contrast(70%)!important;background:#222} html img{-webkit-filter:invert(100%) hue-rotate(0deg) contrast(100%)!important}'; document.head.appendChild(s); } }, { name:'Invert Colors', description:'Inverts all page colors', run:function(){ document.documentElement.style.filter=document.documentElement.style.filter?'':'invert()'; } }, { name:'Tab Cloaker', description:'Changes the tab title and icon', inputs:[{name:'Icon URL',type:'string'},{name:'Tab Title',type:'string'}], run:function(icon,title){ let l=document.querySelector("link[rel*='icon']")||document.createElement('link'); l.type='image/x-icon'; l.rel='shortcut icon'; l.href=icon||'https://www.blooket.com/favicon.ico'; document.head.appendChild(l); document.title=title||'Blooket'; } }, { name:'History Flooder', description:'Floods browser history', inputs:[{name:'Amount',type:'number'}], run:function(n){ const url=location.href; for(let i=1;i<=n;i++) history.pushState(0,0,i===n?url:String(i)); _iAlert('Flooded '+n+' entries!'); } }, ], host:[ { name:'Freeze Timer', description:'Stops the in-game host timer', run:function(){ try{ const s=getStateNode(); clearInterval(s.timerInterval); s.timerInterval=setInterval(()=>s?.getClients?.(false),4000); }catch(e){ _iAlert(e.message); } } }, { name:'Free Player Slots', description:'Unblocks full lobbies', async run(){ try{ const s=getStateNode(); const p=await s.props.liveGameController.getDatabaseVal('c'); let n=0; for(const u in p){ if(s.state.blockedUsers?.includes(u)) continue; const r=await fetch('https://fb.blooket.com/c/firebase/block',{headers:{'Content-Type':'application/json'},method:'POST',body:JSON.stringify({g:s.props.host?.id,u}),credentials:'include'}); if(r.status===200){s.state.blockedUsers?.push(u);n++;} } _iAlert('Freed '+n+' slots!'); }catch(e){ _iAlert(e.message); } } }, { name:'Kick All Players', description:'Removes all players from the game', async run(){ try{ const s=getStateNode(); const d=await s.props.liveGameController.getDatabaseVal(''); s.props.liveGameController.setVal({path:'bu',val:Object.keys(d.c).reduce((o,k)=>(o[k]=1,o),d.bu||{})}); s.props.liveGameController.setVal({path:'c',val:{}}); }catch(e){ _iAlert(e.message); } } }, { name:'Enable Mobile Hosting', description:'Allows hosting on mobile', run:function(){ document.querySelector('meta[name="viewport"]')?.remove(); const m=document.createElement('meta'); m.name='viewport'; m.content='width=1280,initial-scale=1'; document.head.appendChild(m); } }, ], }; const gui = document.createElement('div'); gui.id = 'HazeyGUI'; const titlebar = document.createElement('div'); titlebar.id = 'hgui-titlebar'; const hamBtn = document.createElement('div'); hamBtn.id = 'hgui-hamburger'; hamBtn.title = 'Toggle navigation'; hamBtn.innerHTML = '<span></span><span></span><span></span>'; const titleEl = document.createElement('div'); titleEl.id = 'hgui-title'; titleEl.textContent = "Hazey's Cheats v2.1"; const minBtn = document.createElement('button'); minBtn.id = 'hgui-min'; minBtn.className = 'hgui-wbtn'; minBtn.innerHTML = '−'; minBtn.title = 'Minimise'; const closeBtn = document.createElement('button'); closeBtn.id = 'hgui-close'; closeBtn.className = 'hgui-wbtn'; closeBtn.innerHTML = '✕'; closeBtn.title = 'Close'; titlebar.append(hamBtn, titleEl, minBtn, closeBtn); gui.appendChild(titlebar); const body = document.createElement('div'); body.id = 'hgui-body'; const backdrop = document.createElement('div'); backdrop.id = 'hgui-backdrop'; body.appendChild(backdrop); const panel = document.createElement('div'); panel.id = 'hgui-panel'; const NAV = [ { label:'Overview', items:[ { id:'global', label:'Global', emoji:'🌐' }, { id:'extras', label:'Extras', emoji:'✨' }, { id:'host', label:'Host', emoji:'👑' }, ]}, { label:'Gamemodes', items:[ { id:'gold', label:'Gold Quest', emoji:'🪙' }, { id:'crypto', label:'Crypto Hack', emoji:'💻' }, { id:'fishing', label:'Fishing Frenzy', emoji:'🎣' }, { id:'voyage', label:"Pirate's Voyage", emoji:'🏴‍☠️' }, { id:'defense2', label:'Tower Defense 2', emoji:'🚀' }, { id:'brawl', label:'Monster Brawl', emoji:'⚔️' }, { id:'dinos', label:'Deceptive Dinos', emoji:'🦕' }, { id:'royale', label:'Battle Royale', emoji:'⚡' }, { id:'defense', label:'Tower Defense', emoji:'🏰' }, { id:'cafe', label:'Café', emoji:'☕' }, { id:'factory', label:'Factory', emoji:'🏭' }, { id:'racing', label:'Racing', emoji:'🏎️' }, { id:'kingdom', label:'Crazy Kingdom', emoji:'👑' }, { id:'workshop', label:"Santa's Workshop", emoji:'🎁' }, { id:'flappy', label:'Flappy Blook', emoji:'🐦' }, ]}, ]; const navBtnMap = {}; NAV.forEach(section => { const lbl = document.createElement('div'); lbl.className = 'hgui-sec-label'; lbl.textContent = section.label; panel.appendChild(lbl); section.items.forEach(item => { const btn = document.createElement('button'); btn.className = 'hgui-nav-btn'; btn.dataset.cat = item.id; btn.innerHTML = `<span style="font-size:16px;flex-shrink:0">${item.emoji}</span> ${item.label}`; panel.appendChild(btn); navBtnMap[item.id] = btn; btn.addEventListener('click', () => { selectCategory(item.id, item.label); closePanel(); }); }); }); body.appendChild(panel); const main = document.createElement('div'); main.id = 'hgui-main'; const contentHeader = document.createElement('div'); contentHeader.id = 'hgui-content-header'; const contentTitle = document.createElement('div'); contentTitle.id = 'hgui-content-title'; contentTitle.textContent = 'Select a category →'; contentHeader.appendChild(contentTitle); main.appendChild(contentHeader); const cheatGrid = document.createElement('div'); cheatGrid.id = 'hgui-cheat-grid'; main.appendChild(cheatGrid); body.appendChild(main); gui.appendChild(body); document.body.appendChild(gui); function openPanel() { gui.classList.add('panel-open'); } function closePanel() { gui.classList.remove('panel-open'); } hamBtn.addEventListener('click', ()=>gui.classList.toggle('panel-open')); backdrop.addEventListener('click', closePanel); let minimised = false; minBtn.addEventListener('click', () => { minimised = !minimised; gui.classList.toggle('minimised', minimised); minBtn.innerHTML = minimised ? '+' : '−'; }); closeBtn.addEventListener('click', () => { gui.remove(); STYLE.remove(); }); let drag = null; titlebar.addEventListener('mousedown', e => { if (e.target.closest('button') || hamBtn.contains(e.target)) return; const r = gui.getBoundingClientRect(); drag = { ox: e.clientX - r.left, oy: e.clientY - r.top }; }); document.addEventListener('mousemove', e => { if (!drag) return; gui.style.left = (e.clientX - drag.ox) + 'px'; gui.style.top = (e.clientY - drag.oy) + 'px'; }); document.addEventListener('mouseup', () => drag = null); document.addEventListener('keydown', e => { if (e.ctrlKey && e.key.toLowerCase() === 'e') { e.preventDefault(); gui.style.display = gui.style.display === 'none' ? 'flex' : 'none'; } if (e.ctrlKey && e.key.toLowerCase() === 'x') { e.preventDefault(); gui.remove(); STYLE.remove(); } }); function selectCategory(id, label) { Object.values(navBtnMap).forEach(b => b.classList.remove('active')); if (navBtnMap[id]) navBtnMap[id].classList.add('active'); contentTitle.textContent = label; cheatGrid.innerHTML = ''; const cheats = C[id] || []; if (!cheats.length) { const e = document.createElement('div'); e.style.cssText = 'color:var(--subtext);font-size:14px;padding:20px'; e.textContent = 'No cheats for "' + label + '".'; cheatGrid.appendChild(e); return; } cheats.forEach(cheat => { const card = document.createElement('div'); card.className = 'hgui-card'; const nm = document.createElement('div'); nm.className = 'hgui-card-name'; nm.textContent = cheat.name; card.appendChild(nm); if (cheat.description) { const ds = document.createElement('div'); ds.className = 'hgui-card-desc'; ds.textContent = cheat.description; card.appendChild(ds); } const inputEls = []; (cheat.inputs || []).forEach(inp => { if (inp.type === 'options') { const sel = document.createElement('select'); const populate = opts => opts.forEach(o => { const opt = document.createElement('option'); opt.value = JSON.stringify(o?.value != null ? o.value : o); opt.textContent = o?.name || o; sel.appendChild(opt); }); if (typeof inp.options === 'function') { (async () => { try { populate(await inp.options()); } catch { populate([]); } })(); } else populate(inp.options); card.appendChild(sel); inputEls.push(sel); } else { const input = document.createElement('input'); if (inp.type === 'number') { input.type = 'number'; if (inp.min != null) input.min = inp.min; if (inp.max != null) input.max = inp.max; input.value = inp.value ?? (inp.min ?? 0); } input.placeholder = inp.name || ''; input.style.textAlign = 'center'; card.appendChild(input); inputEls.push(input); } }); const isToggle = cheat.type === 'toggle'; const runBtn = document.createElement('button'); runBtn.className = 'hgui-run-btn' + (isToggle ? (cheat.enabled ? ' ton' : ' toff') : ''); runBtn.textContent = isToggle ? (cheat.enabled ? 'ON' : 'OFF') : 'Run'; runBtn.addEventListener('click', () => { const args = inputEls.map(e => { if (e.type === 'number') return parseInt('0' + e.value); if (e.nodeName === 'SELECT') { try { return JSON.parse(e.value); } catch { return e.value; } } return e.value; }); try { cheat.run.apply(cheat, args); } catch(err) { let errEl = card.querySelector('.hgui-run-err'); if (!errEl) { errEl = document.createElement('div'); errEl.className = 'hgui-run-err'; card.appendChild(errEl); } errEl.textContent = '⚠ ' + (err?.message || 'Needs an active game'); setTimeout(() => errEl.textContent = '', 3000); } if (isToggle) { runBtn.textContent = cheat.enabled ? 'ON' : 'OFF'; runBtn.className = 'hgui-run-btn ' + (cheat.enabled ? 'ton' : 'toff'); } }); inputEls.forEach(e => e.addEventListener('keyup', ev => { if (ev.key === 'Enter') runBtn.click(); })); card.appendChild(runBtn); cheatGrid.appendChild(card); }); } selectCategory('global', 'Global'); })();
